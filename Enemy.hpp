#include <SFML/Graphics.hpp>

using namespace sf;

class ENEMY
{
   void set(Texture &image, int x, int y);
   void update(float time);

   void update(float time);

   void Collision();
}
